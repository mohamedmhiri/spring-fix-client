quickfixj:
  client:
    config: quickfixj-client.cfg
    jmx-enabled: true
  server:
      config: quickfixj-server.cfg
      jmx-enabled: true


server:
  port: 8080

management:
  security:
    enabled: false

endpoints:
  quickfixjclient:
    id: qfjclient
    enabled: true
    sensitive: false